[build]
  command = "npm ci && npm run build"
  publish = "dist"



# Helpful environment: ensures Netlify uses the same Node, and avoids
# accidental behavior from plugin default config. Adjust if your build
# command or publish directory is different.
